// A interface do wizard é hospedada pelo layout persistente /pgr/[id].
// Esta página existe apenas para materializar/validar o segmento [etapa].
export default function PgrEtapaPage() {
  return null;
}
